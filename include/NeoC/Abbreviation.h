#pragma once

/* ------------------------------------------------------ */
/* 可変長引数使用開始宣言 */
#define use(FMT_STR) va_list ap; va_start(ap, FMT_STR);

/* 可変長引数使用終了宣言 */
#define release va_end(ap);
/* ------------------------------------------------------ */
